---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: objectsets.package.r
spec:
  group: package.r
  names:
    kind: ObjectSet
    listKind: ObjectSetList
    plural: objectsets
    singular: objectset
  scope: Namespaced
  versions:
  - name: alphav1
    schema:
      openAPIV3Schema:
        description: ObjectSet is the Schema for the filesets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ObjectSetSpec defines the desired state of ObjectSet
            properties:
              allowedRoles:
                items:
                  type: string
                type: array
              filter:
                type: string
              friendlyName:
                type: string
              sourceName:
                type: string
            required:
            - filter
            - sourceName
            type: object
          status:
            description: ObjectSetStatus defines the observed state of ObjectSet
            properties:
              secretName:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
